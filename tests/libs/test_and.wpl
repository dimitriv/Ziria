#include <externals.blk>
#define SIZE 288


fun comp test_and() {
	

	var y: arr[SIZE] bit;
	do{
		for n in [0,SIZE]
		y[n] := '1;	
	};

	repeat{
		x <- takes SIZE; 
		emits v_and(x,y)
	}
}

let comp main = read[bit] >>> test_and() >>> write[bit]
