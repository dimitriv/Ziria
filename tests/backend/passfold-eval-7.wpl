-- Let a long loop

fun f() {
  var x : int := 0;
  for i in [0, 50000] {
    x := x + 1;
  }
  return x;
}

fun comp test() {
  a <- take;
  emit a;
  emit f();
  emit a; 
}

let comp main = read[int] >>> test() >>> write[int];
