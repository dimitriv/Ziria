let comp cntRxPkts() =
var lastNo : int := -1;

repeat{
  x <- take;
  emit lastNo;
  do{
    lastNo := x;
  }
}
in


read[int] >>> 
  repeat{
    x<-take;
    emit x;
  }
  >>> cntRxPkts()
>>> write[int]
