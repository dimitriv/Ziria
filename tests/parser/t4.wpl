var x : int; 
var y : bool;
var z : complex32;

let foo(x : int) = 
   var z : bool := true;
   return (x+1);
in

let comp bar(x : int) = 
  seq { x <- take
      ; emit x
      } >>> map foo
in

read [int] >>> map foo >>> write [int] 
