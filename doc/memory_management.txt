Notes on memory menagement:

(particular focus is on arrays)

Input: Drivers have there own buffer for reading, of max size 2048B. 
This buffer is then used by the WPL code.
*** No memcpy here, but bind will copy into doneVal



